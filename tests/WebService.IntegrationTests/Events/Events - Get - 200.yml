info:
  name: Events - Get - 200
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/events/:eventid"
  params:
    - name: eventid
      value: 95f541b3-0925-4b66-a746-1d97df7d3ba5
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test('Status code is 200 OK', function(){
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
