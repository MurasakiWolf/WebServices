info:
  name: Events - Get - 200
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/events/:eventid"
  params:
    - name: eventid
      value: 2762e039-0f6c-462c-9252-2b15f6848f92
      type: path
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test('Status code is 200 OK', function(){
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
